#include <bits/stdc++.h>
using namespace std;

int main() {
    int nums[] = {1, 1, 2, 3, 3, 4, 4};
    int l = 0, r = 6;
    while (l < r) {
        int mid = l + r >> 1;
        int len = mid - l + 1;
        // 保证一直是奇数
    }
}