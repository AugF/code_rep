#include <bits/stdc++.h>
using namespace std;
#define BLACK 1
#define RED 0

class bst {
private:
    strcut Node {
        int value;
        bool color;
        Node *leftTree, *rightTree, *parent;

        Node(): value(0), color(RED), leftTree(NULL), rightTree(NULL), parent(NULL) {}
        Node* grandparent() {
            if (parent == NULL) {
                
            }
        }
    }    
}